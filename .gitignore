node_modules/
.pnpm-store/
dist/
build/
coverage/
playwright-report/
test-results/
.env
.env.*
!.env.example
client_secret_*.json
# Downloaded gateway/API key exports — never commit secrets.
*_api_keys_*.csv
*api_key*.csv
.venv/
.media/
__pycache__/
.pytest_cache/
.ruff_cache/
.pyright/
.wrangler/
.dev.vars
*.log
.DS_Store
Thumbs.db
.react-router/
uv.lock.tmp
.claude/
# Ad-hoc screenshots/generated images dropped at the repo root instead of a
# real assets folder — never intentional, not referenced by any app.
/*.png
/*.jpg
/*.jpeg
/*.webp
# Playwright CLI's own snapshot/screenshot artifacts from manual verification
# sessions — debugging output, never referenced by the app or its tests.
.playwright-cli/
.playwright-mcp/
